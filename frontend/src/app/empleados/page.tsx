import UserList from "@/components/UserList";
import React from "react";

const EmpleadosPage = () => {
  return (
    <div>
      <UserList params={"empleados"} />
    </div>
  );
};

export default EmpleadosPage;
